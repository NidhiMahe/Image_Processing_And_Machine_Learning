# Install OpenCV (main and headless version for environments without GUI)
pip install opencv-python
pip install opencv-python-headless

# Install scikit-image and NumPy for image processing
pip install scikit-image
pip install numpy

# Install matplotlib for visualization
pip install matplotlib

# Install face_recognition library for facial detection and encoding
pip install face_recognition

# Install facenet-pytorch for deep learning-based face recognition
pip install facenet-pytorch
pip install torch
pip install Pillow

# Install DeepFace for high-level face analysis
pip install deepface

# OpenCV
import cv2

# scikit-image
from skimage import io, filters, draw

# NumPy and Matplotlib
import numpy as np
import matplotlib.pyplot as plt

# face_recognition
import face_recognition

# facenet-pytorch
import torch
from facenet_pytorch import InceptionResnetV1
from PIL import Image

# DeepFace
from deepface import DeepFace
